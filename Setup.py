from setuptools import setup, find_packages

setup(
    name="rankvision",
    version="0.1.0",
    description="Rank Vision: Amazon sales rank tracking, forecasting, and AI recommendations",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.10",
)
